# 墨阅 · Markdown 解析器

本地 Markdown 阅读与编辑工具：拖入 / 选择文件或文件夹即可解析渲染，支持脚注、数学公式（MathJax）、Mermaid 图表、代码高亮、缩进层级列表、表格、任务清单等，并内置编辑模式（实时渲染、双向同步滚动、可保存）。**零联网、零外部依赖**，所有解析、渲染与编辑都在本机完成。

> 支持的全部写法、边界与限制见 **[语法支持.md](语法支持.md)**，同时请用本项目打开这个文件以获得最佳体验。

## 编辑器：Monaco 内核

编辑区基于 [Monaco Editor](https://microsoft.github.io/monaco-editor/)（VS Code 同款内核），并打包为**离线内嵌版本**（编辑器内核、worker、样式全部本地化，无任何外部请求）：

- **缩进折行**：开启 `wordWrap` + `wrappingIndent: same`，长行自动换行时，续行与逻辑行行首缩进对齐，不再顶格
- 编辑区内选中文字可直接拖拽移动（移动而非复制，拖动时显示虚线落点预览）
- 外部文本可拖入编辑区，光标实时跟随、在落点插入
- 词法高亮与阅读区配色统一（标题 / 链接 / 列表等使用主题强调色），跟随亮 / 暗色主题切换

## 渲染：全部本地

- Markdown 解析（表格、任务清单、脚注、嵌套列表、引用块、代码块……）
- **换行按原文保留**：段落里的普通换行（软换行）渲染为可见换行，多行文本不再被压成一行；行尾两空格仍是硬换行
- **脚注双向跳转**：正文上标 → 脚注解释，脚注末尾的 `↩` → 跳回正文引用处（同一脚注被引用多次时，每个引用点各有自己的 `↩`）
- **列表按 CommonMark 分块**：相邻的有序 / 无序列表各自成块（有序项保留编号），不会因为中间一个空行就让整块列表被判定为 loose、每项套上一层 `<p>` 而突然拉大高度
- **标题锚点**：各级标题自动生成 GitHub 风格 `id`（中文保留、重名依次 `-1`/`-2`），正文里的 `[文字](#标题)` 可以跳转，落点放在视口 20% 高度处并高亮，标题下方的内容能一次看全。锚点匹配很宽容——GitHub 的 `#21-A--B--C--D`、本程序生成的 `#21-A-B-C-D`（以及去掉标点 / 大小写不同 / URL 编码）都能命中同一个标题
- **隐藏内容遮罩**：`||内容||`（Discord 同款）渲染成灰条遮罩，点击或回车显示；支持嵌套强调、行内代码，`\|\|` 可转义，代码块与数学公式里的 `||` 不受影响
- **链接 / 图片目标**：支持 CommonMark 的尖括号写法 `[文字](<带 空格的目标> "标题")`，也支持直接写空格（`[文字](#2.1 A / B)`、`![图](图片 名.png)`）；标题用 `"…"` / `'…'` / `(…)`；URL 里的 `&` 不会被双重转义，`javascript:` 等危险协议一律拦掉
- **图片路径**：相对路径（`a.svg` / `./a.svg` / `../a.svg`）与根路径（`/a.svg`）都能正常加载——根路径按「相对本程序」解析，不再指到磁盘根；独占一段或独占一个列表项的小图标（如自带 `width="1em"` 的 SVG）有最小显示尺寸
- MathJax（SVG 输出）渲染行内 / 块级数学公式；块级公式按容器宽度等比缩放，不出横向滚动条；补齐标准 LaTeX 未定义的大写希腊字母（`\Alpha` `\Zeta` `\Kappa` …）
- Mermaid 渲染图表，适配亮 / 暗主题（切换主题自动重绘），点击可放大查看
- 代码高亮（24 种语言，逐语言词法：关键字 / 函数名 / 变量 / 字符串 / 数字 / 注释 / 标签 / 属性各有配色；代码块统一深色底，亮暗主题观感一致）
- **代码块显示模式**（右上角语言名左边的按钮切换，DS 版在「复制」左边）：**普通模式** = 不换行、不做缩进、长行横向滚动、原样保留（ASCII 流程图 / 管线图 / 目录树用），**代码模式** = 自动换行 + 续行缩进。不带语言名或语言名为 `text` 的块默认普通模式，带其它语言名的块默认代码模式；手动切换跟着文件走，切文档、进出编辑模式都不会丢
- **代码块长行软换行**（代码模式）：换行后的续行缩进 = 该行自身的缩进（行首 0 空格顶格、4 空格缩进 4 字符……），与编辑区 Monaco 的 `wrappingIndent: same` 语义一致
- **代码块角标操作**：右上角显示语言名（未写语言名显示 `text`），**悬停或点击角标**弹出「复制」「下载」按钮（下载为 `snippet.<后缀>`）

## 交互细节

- 亮 / 暗主题切换带颜色过渡动画（不在首屏播放；系统开启「减弱动态效果」时自动退化为瞬切）
  - 主题过渡走 **View Transition 整屏合成淡入淡出**：重活（重绘 mermaid、给 Monaco 换主题、长文档重排）都在切换回调里做完，再整屏交叉淡化——不再给每个 DOM 节点挂 transition，所以十万字长文档、整篇一个大代码块、几十张图表都不掉帧；mermaid 只重绘**视口附近**的图，其余的滚到附近自动补齐。不支持 View Transition 的浏览器自动退回逐元素过渡，并把代码块 / 图表的子树排除在过渡之外
- 拖入 / 选择新文件后立刻切换到新文件浏览（正在看别的文档时同样切换）；已打开文档时想继续追加，用右上角**「更多」→「导入新文档 / 导入新文件夹」**，不必先清空列表
- 在文件库里切换文档后，新文档从**顶部**开始阅读
- 进出编辑模式保持阅读位置：切到编辑时编辑区与预览都停在你正在看的那一段，退出时回到原处（不再每次都弹回顶部；整篇就是一个大代码块、视口停在块中间时同样准确）
- 二级菜单适配手机端返回键：更多菜单 / 文件库 / 图表放大浮层打开时，按返回键只关闭该层，不会直接退出页面；层全部关掉后返回键才正常后退（用按钮、ESC、点遮罩关闭时会把占用的历史记录一并回退，不会多出一次「空按」）
- 图表放大浮层用不透明的主题卡片底色，亮色主题下烘死在 SVG 里的深色连线依然清晰

## 两个版本

| 文件 | 说明 |
|---|---|
| `index.html` | 原版：需与 `lib/` 目录同放，便于日常维护与二次开发 |
| `InkMark.html` | **单文件版**：Monaco 内核、worker、MathJax、Mermaid、样式全部内联进一个 HTML |

两者功能完全一致，任选其一。

## 快速开始

1. **电脑**：下载并打开 release 里的 HTML 文件，用浏览器打开即可使用（Windows / macOS / Linux 均可）
2. **手机（安卓）**：把 release 里的 HTML 文件传到手机，用文件管理器找到它并用浏览器打开（微信内置浏览器也可运行）
3. **导入文档**：把 `.md` 文件拖进窗口（支持一次多份或整个文件夹），或在空状态点击按钮选择；无法拖入且已经打开着文档时，用右上角「更多」→「导入新文档 / 导入新文件夹」继续追加
4. **编辑模式**：右上角「编辑」按钮进入编辑模式，左侧改动实时渲染到右侧，两侧滚动联动，可保存回本地（下载）
5. 在线版（GitHub Pages）：[InkMark](https://reinlink.github.io/InkMark/) 

## 项目结构

```
Markdown解析/
├── index.html                         ← 目录版主程序
├── lib/
│   ├── mermaid.min.js                 ← 图表库（本地）
│   ├── mathjax-tex-svg.js             ← 数学公式库（本地，SVG 输出无需字体）
│   └── monaco/
│       ├── editor.bundle.js           ← Monaco 编辑器离线包（含 worker，Blob 内联）
│       └── editor.bundle.css          ← 编辑器样式（字体 data URI 内联）
├── editor-build/                      ← Monaco 离线包构建工程（维护用）
├── InkMark.html                       ← 单文件离线版（构建产物，在 releases 里）
├── 构建单文件版.js                     ← 单文件版构建脚本（Node.js）
├── 语法支持.md                         ← 语法支持清单（支持的全部写法 + 限制 + 与标准的差异）
└── 测试.md                            ← 功能覆盖测试样例
```

## 隐私

所有解析与渲染均在本机完成，文件内容不会上传、不联网。
